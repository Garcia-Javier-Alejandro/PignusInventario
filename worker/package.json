{
  "name": "@pignus/inventario-worker",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply pignus-inventario-db",
    "migrate:remote": "wrangler d1 migrations apply pignus-inventario-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.80.0"
  }
}
